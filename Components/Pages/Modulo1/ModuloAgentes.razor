@page "/agentes"

<!-- Título y descripción del módulo -->
<h1>Módulo de Agentes</h1> <!-- Título del módulo -->
<p>Bienvenido al módulo de agentes. Aquí puedes gestionar las multas, ver el mapa de multas y revisar las comisiones.</p> <!-- Descripción del módulo -->

<!-- Lista de enlaces para acceder a las diferentes secciones del módulo -->
<ul>
    <li id="registro">
        <a href="/registro-multa">
            <div class="panel">
                <img src="Modulo1/multa.jpeg" alt="Registrar Multa" />
                <div class="text">
                    <h3>Registrar Multa</h3>
                    <p>Registrar nuevas multas y gestionarlas.</p>
                </div>
            </div>
        </a>
    </li>
    <li id="mapa">
        <a href="/mapa-multas">
            <div class="panel">
                <img src="Modulo1/mapa.jpg" alt="Mapa de Multas" />
                <div class="text">
                    <h3>Mapa de Multas</h3>
                    <p>Ver todas las multas registradas en el mapa.</p>
                </div>
            </div>
        </a>
    </li>
    <li id="comisiones">
        <a href="/comisiones">
            <div class="panel">
                <img src="Modulo1/comisiones.webp" alt="Comisiones" />
                <div class="text">
                    <h3>Comisiones</h3>
                    <p>Revisar las comisiones asociadas a las multas.</p>
                </div>
            </div>
        </a>
    </li>
</ul>


<style>
/* Estilo para el contenedor de los enlaces (ul) */
ul {
    display: flex; /* Usamos flexbox para alinear los elementos horizontalmente */
    justify-content: space-around; /* Espaciado entre los elementos */
    list-style-type: none; /* Eliminar los puntos de la lista */
    padding: 0; /* Eliminar el padding por defecto */
}

/* Estilo para los paneles dentro de los enlaces */
.panel {
    width: 250px; /* Ancho del panel */
    height: 300px; /* Alto del panel */
    background-color: #369e053b; /* Color de fondo para los paneles */
    display: flex;
    flex-direction: column; /* Para que la imagen y el texto estén verticalmente alineados */
    justify-content: center; /* Centrado vertical */
    align-items: center; /* Centrado horizontal */
    border-radius: 10px; /* Bordes redondeados */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Sombra suave */
    cursor: pointer;
    position: relative;
    overflow: hidden; /* Evitar que el contenido se desborde */
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Transición suave */
    text-align: center; /* Centrado del texto */
    padding: 10px;
}

/* Estilo para las imágenes dentro del panel */
.panel img {
    width: 100%;
    height: 150px;
    object-fit: cover; /* Ajusta la imagen sin distorsionarla */
    border-radius: 10px;
    transition: transform 0.3s ease; /* Efecto de transición en la imagen */
}

/* Estilo para el texto dentro del panel */
.panel .text {
    margin-top: 10px;
}

.panel .text h3 {
    font-size: 18px;
    font-weight: bold;
    color: #333;
    margin: 0;
}

.panel .text p {
    font-size: 14px;
    color: #666;
    margin: 5px 0 0;
}

/* Efecto hover para el panel */
#registro:hover .panel,
#mapa:hover .panel,
#comisiones:hover .panel {
    transform: translateY(-10px); /* Eleva el panel al pasar el cursor */
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2); /* Sombra más fuerte */
}

/* Efecto hover para la imagen dentro del panel */
#registro:hover .panel img,
#mapa:hover .panel img,
#comisiones:hover .panel img {
    transform: scale(1.1); /* Hace que la imagen se agrande ligeramente */
}

/* Estilo cuando pasa el cursor sobre el enlace con ID específico */
#registro a:hover .text h3,
#mapa a:hover .text h3,
#comisiones a:hover .text h3 {
    color: #2aad09; /* Cambiar color del título al pasar el cursor */
}

/* Opcional: Estilo para los enlaces para que no se vean subrayados */
a {
    text-decoration: none;
}



</style>
